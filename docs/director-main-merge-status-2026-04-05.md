# Director main merge status (2026-04-05)

- Source branch used for merge: `work`.
- `main` branch has been fast-forwarded to the latest `work` commit.
- Production deployment verification is blocked in this environment (no access to Vercel/GitHub network).
- No `app/director` route files exist in this checkout, so `/director`, `/director/cases`, and `/director/cases/new` are not present in code here.
